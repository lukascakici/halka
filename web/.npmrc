# @vercel/analytics declares optional peers for many frameworks (incl. SvelteKit),
# which npm 11 tries to resolve and pulls a conflicting Vite. We don't use those
# frameworks, so skip optional-peer auto-resolution. Keeps local and CI in sync.
legacy-peer-deps=true
